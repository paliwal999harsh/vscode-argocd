---
description: 'Delete a Kubernetes cluster from ArgoCD using the VS Code extension, removing it from ArgoCD management and requiring confirmation to prevent accidental removal of deployment targets.'
---

# Delete Cluster

**Description:** Delete a Kubernetes cluster from ArgoCD using the VS Code extension, removing it from ArgoCD management and requiring confirmation to prevent accidental removal of deployment targets.

**Type:** Context Menu (Clusters view/item)

**Keyboard Shortcut:** None

## Usage Flow

1. Open Command Palette → "ArgoCD: Delete Cluster" or right-click on a cluster
   ![](../../assets/commands/cluster/delete-cluster-step1.jpg)
2. Select the cluster to delete
   ![](../../assets/commands/cluster/delete-cluster-step2.jpg)
3. Confirm the deletion
   ![](../../assets/commands/cluster/delete-cluster-step3.jpg)
