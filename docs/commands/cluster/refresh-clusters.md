---
description: "Refresh the status and availability information of all Kubernetes clusters registered with ArgoCD, updating cached metadata and connectivity status in the extension's Clusters view."
---

# Refresh Clusters

**Description:** Refresh the status and availability information of all Kubernetes clusters registered with ArgoCD, updating cached metadata and connectivity status in the extension's Clusters view.

**Type:** Command Palette, Context Menu (Clusters view)

**Keyboard Shortcut:** None

## Usage Flow

1. Open Command Palette → "ArgoCD: Refresh Clusters" or right-click on Clusters view
   ![](../../assets/commands/cluster/refresh-clusters-step1.jpg)
2. Wait for all clusters to be refreshed
   ![](../../assets/commands/cluster/refresh-clusters-step2.jpg)
