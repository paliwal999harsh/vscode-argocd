---
description: 'Add a Git/Helm/OCI repository to ArgoCD, validate connectivity, and show it in the Repositories view. Prompts for repository URL and authentication, performs validation checks, and registers the repository with ArgoCD so it appears in the Repositories view.'
---

# Add Repository

**Description:** Add a Git/Helm/OCI repository to ArgoCD, validate connectivity, and show it in the Repositories view. Prompts for repository URL and authentication, performs validation checks, and registers the repository with ArgoCD so it appears in the Repositories view.

**Type:** Command Palette, Context Menu (Repositories view/item)

**Keyboard Shortcut:** None

## Usage Flow

1. Open Command Palette → "ArgoCD: Add Repository"
   ![](../../assets/commands/repository/add-repository-step1.jpg)
2. Enter repository details
   ![](../../assets/commands/repository/add-repository-step2.jpg)
3. Confirm and wait for validation
   ![](../../assets/commands/repository/add-repository-step3.jpg)
