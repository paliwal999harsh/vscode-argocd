---
description: 'View the Kubernetes manifests and resource definitions deployed by an ArgoCD application, inspecting YAML/JSON objects that ArgoCD has synchronized or is tracking in the target cluster.'
---

# View Application Manifests

**Description:** View the Kubernetes manifests and resource definitions deployed by an ArgoCD application, inspecting YAML/JSON objects that ArgoCD has synchronized or is tracking in the target cluster.

**Type:** Tree Item Click (Applications view/item)

**Keyboard Shortcut:** None

## Usage Flow

1. Open Command Palette → "ArgoCD: View Application Manifests" or right-click on an application
   ![](../../assets/commands/application/view-application-manifests-step1.jpg)
2. Select the application to view manifests for
   ![](../../assets/commands/application/view-application-manifests-step2.jpg)
3. View the manifests in a new editor window
   ![](../../assets/commands/application/view-application-manifests-step3.jpg)
