---
description: 'Switch the active ArgoCD server connection used by the extension, updating the context for subsequent commands and views so operations target the selected ArgoCD instance with appropriate credentials and endpoint.'
---

# Switch Connection

**Description:** Switch the active ArgoCD server connection used by the extension, updating the context for subsequent commands and views so operations target the selected ArgoCD instance with appropriate credentials and endpoint.

**Type:** Command Palette

**Keyboard Shortcut:** None

## Usage Flow

1. Open Command Palette → "ArgoCD: Switch Connection" or right-click on a connection
   ![](../../assets/commands/connections/switch-connection-step1.jpg)
2. Select the connection to switch to
   ![](../../assets/commands/connections/switch-connection-step2.jpg)
3. Wait for the extension to connect to the new server
   ![](../../assets/commands/connections/switch-connection-step3.jpg)
